

const NetworkItem = (props) => {
    return(
        <li className="network-item">
            <span>
                {props.name}
            </span>
        </li>
    )
}

export default NetworkItem